const initialState = {
  data: {},
  isLoading: false,
  isError: false,
  isSuccess: true,
};

const item = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default item;
